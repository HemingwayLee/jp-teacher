# URL of your local Ollama server.
# Defaults to host.docker.internal:11434 (host machine from inside Docker).
# Change to http://localhost:11434 when running without Docker.
OLLAMA_HOST=http://host.docker.internal:11434

# Ollama model to use for chat and translation
OLLAMA_MODEL=gemma3:12b
